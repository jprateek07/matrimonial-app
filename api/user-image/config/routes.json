{
  "routes": [
    {
      "method": "GET",
      "path": "/user-images",
      "handler": "user-image.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/user-images/count",
      "handler": "user-image.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/user-images/:id",
      "handler": "user-image.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/user-images",
      "handler": "user-image.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/user-images/:id",
      "handler": "user-image.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/user-images/:id",
      "handler": "user-image.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
